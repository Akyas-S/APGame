package Mainmenu;

import javax.swing.*;

public class Store {
    JFrame StoreWind = new JFrame("Store");

    Store(){
        StoreWind.setBounds(0, 0, 480, 800);
        StoreWind.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        StoreWind.setSize(480, 800);
        StoreWind.setLocationRelativeTo(null);
        StoreWind.setLayout(null);
        StoreWind.setVisible(true);
    }
}
