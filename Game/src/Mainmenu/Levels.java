package Mainmenu;

import javax.swing.*;

public class Levels  {
    JFrame LevelsWind = new JFrame("Levels");

    Levels(){
        LevelsWind.setBounds(0, 0, 480, 800);
        LevelsWind.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        LevelsWind.setSize(480, 800);
        LevelsWind.setLocationRelativeTo(null);
        LevelsWind.setLayout(null);
        LevelsWind.setVisible(true);
    }
}
